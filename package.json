{
  "name": "frame-sense-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k false --restart-tries 1000 --restart-delay 1000 -n \"web,api\" -c \"blue.bold,green.bold\" \"pnpm --filter web dev\" \"pnpm --filter api dev\"",
    "build": "pnpm --filter web build",
    "install:api": "node scripts/setup-api.js",
    "setup": "pnpm install && pnpm install:api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
